# Alpha Release Changes

http://34.158.47.112/schoolcatering\
Alpha release discussion on 3:00pm 16th March 2026.\
Updated: 3:00pm, 17th March 2026.\
Target change completion: 19th March 2026.\

## Homepage

1. Homepage hero text is `Meals For Students, Up To Five Dishes Per Meal, Register or Log In.`
2. Extra menu guidance sentence is removed from the hero section.
3. `Log In` and `Register` buttons fill the full row properly.
4. Google Log In is disabled so the missing client ID error is no longer visible.
5. Homepage login uses the single shared URL: `/schoolcatering/login`.

## Login And Registration

6. Login uses a shared `SINGLE SIGN ON (SSO)` page for Family, Student, Delivery, Kitchen, and Admin.
7. Shared login page shows the updated temporary login examples.
8. Seeded/backdoor passwords use `Teameditor@123` for Admin, Kitchen, Delivery, Family, and Student system access where requested.
9. Registration flow uses the updated field order and labels.
10. `Registrant User` options are `Parent`, `Student`, and `Guardian/Teacher`.
11. `Guardian/Teacher Name` and `Guardian/Teacher Phone Number` are included.
12. Registration terminology uses Family Group and Student wording.
13. Autogenerated password behavior is removed from registration.
14. Password validation requires at least 6 characters, 1 uppercase letter, 1 number, and 1 symbol.

## Family And Student Modules

15. Old Parent/Youngster screens are replaced with Family/Student screens.
16. Family and Student main pages use icon-based navigation.
17. Family and Student main page layouts are adjusted for mobile-first display.
18. Hub `Home` action is renamed to `Logout` where applicable.
19. Overview pages use calendar-style order highlighting.
20. Order, Billing, Rating, and Menu pages are set up under Family and Student.
21. Family and Student inner pages use bottom non-sticky `Return` buttons.
22. `Logout` appears only on Family and Student main pages.
23. Record pages exist at:
   - `/schoolcatering/family/consolorder`
   - `/schoolcatering/student/consolorder`
24. Consolidated order sections are removed from the main order pages and moved into the new Record pages.
25. Family and Student order record pages allow service date filtering.
26. Student order record pages include quick date buttons.
27. Family order record edit goes directly to the Family order page with the correct date and draft loaded.
28. Family and Student logout fully clears the login session.
29. Moving away from Family and Student inner pages, including Rating, keeps users logged in and returns them to their main page through the Return flow.
30. When the Draft section has items, leaving or refreshing the order page shows a confirmation message to Place Order or Discard Draft.
31. Overview calendars show meal names on highlighted order days, including Lunch, Snack, and Breakfast where applicable.

<div style="page-break-after: always;"></div>

## Menu And Session Display

32. Session colors are used across the app for Lunch, Snack, and Breakfast.
33. Session colors apply to menu cards, order cards, billing cards, session badges, and overview calendar highlights.
34. Public Menu page includes session filtering.
35. Public Menu page uses a dropdown instead of session radio buttons.
36. Snack and Breakfast appear in the dropdown only when those sessions are active.
37. Menu page return behavior sends logged-in users back to Family or Student modules instead of the homepage.

## Admin Module

38. Admin main page uses the requested 3-column button layout.
39. Admin Orders page and Orders button are included.
40. Admin Billing button and Billing page access are restored.
41. Admin Rating page is included, and menu ratings are moved out of Admin Menu into Admin Rating.
42. Bottom `Return` button placement is consistent inside the main cards on Admin inner pages.
43. Admin can read and delete orders from Admin Orders.
44. Deleted orders no longer continue appearing in Admin Delivery assignments.
45. Admin Student wording uses Student instead of Youngster.
46. Admin Family wording uses Family instead of Parent where requested.
47. `Show ID` and `Show PW` pop-up boxes in Admin Student use Student and Family labels.
48. `Show ID` and `Show PW` pop-up boxes in Admin Family use Family and Student labels.
49. `Show Teacher/Guardian` button exists in Admin Family with a pop-up box showing stored teacher/guardian details.
50. Admin Schools includes a configurable `Cut Off Time` card.
51. Admin can see stored passwords in the Show Password flows for Family and Student records.
52. Admin password reset uses `Set new Password` with manual entry instead of autogenerated passwords.

## Kitchen Module

53. Kitchen main overview page includes:
   - Yesterday | Today | Tomorrow | Select Date
54. Today is the default overview state.
55. Kitchen inner pages show the relevant date or service date without repeating the top buttons.
56. `Refresh` and `Download PDF` remain on all Kitchen inner pages.
57. Kitchen action buttons follow the requested two-line layout before the main overview page change.
58. Kitchen `Download PDF` follows the active selected date.
59. Kitchen orders begin in `Order Pending`.
60. Orders move to `Order Completed` only when manually marked complete.
61. Completed orders stay completed until manually reverted.
62. Long words like `Yesterday` and `Tomorrow` do not break across lines in mobile view.

<div style="page-break-after: always;"></div>

## Delivery Module

63. Delivery main overview page includes:
   - Yesterday
   - Today
   - Tomorrow
   - Select Date
64. Delivery overview matches Kitchen behavior.
65. `Download PDF` remains on all Delivery inner pages.
66. Delivery `Download PDF` follows the active selected date.
67. Delivery layout shows `Order Pending` and `Order Completed` as left and right columns.
68. Delivery `Confirmation Note` saves automatically each day.
69. Admin can view saved Delivery confirmation notes by service date.
70. Long words like `Yesterday` and `Tomorrow` do not break across lines in mobile view.

## Data, Seeds, And Backend Rules

71. Old family/student sample accounts are replaced with the requested Family/Student demo data pattern.
72. Sample records remain normal data that Admin can create, edit, and delete instead of fixed locked records.
73. School sample setup does not create duplicate school records.
74. System login redirects use the new Family and Student pages instead of deleted old paths.
75. Student deletion rules match active visible orders and billing.
76. Student deletion no longer crashes because of old cart and order cleanup.
77. Family deletion rules match linked student order and billing history.
78. Delivery assignment queries exclude deleted/cancelled orders.
79. Order cutoff handling uses the cutoff time set by Admin instead of a fixed 8:00am.
80. Family and Student demo sample data follows the requested family and student naming, school, age, grade, allergy, and registrant patterns for normal Admin create, edit, and delete use.

--- END ---
